var filteredWarnings = ['@walletconnect/web3-provider'];

var filterConsole = function () {
  var warnFn = console.log;

  console.warn = function () {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    if (typeof args[0] === 'string' && filteredWarnings.filter(function () {
      return args[0].includes(args[0]);
    }).length > 1) {
      return;
    }

    return warnFn;
  };
};

module.exports = {
  filterConsole: filterConsole
};